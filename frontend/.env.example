# Frontend production env (set these in Vercel → Project → Settings → Environment
# Variables). Leave UNSET for local dev — Vite proxies /api and /ws to :8000.
VITE_API_URL=https://restaurant-ims-backend.onrender.com/api
VITE_WS_URL=wss://restaurant-ims-backend.onrender.com
